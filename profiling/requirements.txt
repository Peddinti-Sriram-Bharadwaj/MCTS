# Neural MCTS — AlphaZero-style with JAX
# ----------------------------------------
# Python 3.10+
#
# Install:
#   pip install -r requirements.txt
#
# JAX CPU (default, works everywhere):
#   pip install -r requirements.txt
#
# JAX GPU (NVIDIA CUDA 12):
#   pip install -r requirements.txt
#   pip install -U "jax[cuda12]"
#
# JAX GPU (Apple Silicon / Mac MPS):
#   pip install -r requirements.txt
#   pip install jax==0.10.2 jaxlib==0.10.2 jax-mps==0.10.10
#   (Run with `JAX_PLATFORMS=mps python ...`)

jax
jaxlib
flax>=0.8.0
optax>=0.2.2
orbax-checkpoint>=0.6.0
numpy>=1.24
